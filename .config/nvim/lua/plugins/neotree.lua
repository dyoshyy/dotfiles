return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    sync_root_with_cwd = false,
    respect_buf_cwd = false,
    update_focused_file = {
      enable = false,
      update_root = false,
    },
  },
}
